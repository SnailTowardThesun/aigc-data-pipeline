version: "1.0"
services:
  mongo:
    image: mongo:latest
    container_name: mongo
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: aigc
      MONGO_INITDB_ROOT_PASSWORD: aigc-123